import { PKConfig } from '@core/config';

export const environment: PKConfig = {
  mode: 'dev',
  development: true,
  apiBaseUrl: 'http://localhost:3000/api',
  appVersion: '1.0.0',
  apiKey: '123',
};
