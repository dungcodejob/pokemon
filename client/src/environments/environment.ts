import { PKConfig } from '@core/config';

export const environment: PKConfig = {
  mode: 'prod',
  development: false,
  apiBaseUrl: 'https://api.pokemon.com',
  appVersion: '1.0.0',
  apiKey: '123',
};
